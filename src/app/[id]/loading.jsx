import Loader from "@/component/Loader/Loader";
import React from "react";

const loading = () => {
  return (
    <div>
      <Loader type="animeInfo" />
    </div>
  );
};

export default loading;
